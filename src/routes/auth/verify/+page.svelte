<!-- The load function always redirects; this renders only if something goes
     very wrong, so it just needs to not look broken. -->
<div class="mx-auto max-w-sm px-6 pt-24">
	<p class="text-sm text-muted">Signing you in…</p>
</div>
